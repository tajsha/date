<%= form_for(:question, :url => question_answers_path(question)) do |f| %>
	<ul>
		<%= f.text_area :answer, rows: 4, style: 'width: 95%'%>
		<%= f.hidden_field :recipient_id %>
		<%= f.submit "Submit", class: 'btn btn-primary' %> 
		</ul>
		<% end %>