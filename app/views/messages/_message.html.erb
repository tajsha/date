<div class="message">
  <div class="created_at"><%= message.created_at.present? ? message.created_at.strftime("%B %d, %Y") : message.created_at %></div>
  <div class="content">
    <%=h message.body %>
  </div>
</div>